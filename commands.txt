# backend api
	py -m uvicorn backend.main:app --reload --reload-dir backend --port 8000

# frontend
	py -m streamlit run frontend/app.py

# benchmark testing command:
	python evaluation/run_benchmark.py